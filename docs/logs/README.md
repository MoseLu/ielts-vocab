# Logs Docs

Use this folder for append-only execution records.

## Subfolders

- `submit/`: commit-batch submission records and similar immutable logs

Logs should record what happened, not what should happen next. Future work belongs in `docs/planning/`.
