# Milestone
Last updated: 2026-04-10 21:01:21 +08:00

## Current Milestone
- Complete the backend transition from a split-service runtime into a classical microservice architecture while keeping the already deployed remote backend stable: `gateway-bff` remains the only browser ingress, service-owned PostgreSQL boundaries replace shared persistence, shared platform support modules absorb monolith helpers, Redis and RabbitMQ become real infrastructure dependencies, event-driven read models replace shared reads, and the monolith is retired only after remote rollout, smoke verification, and rollback paths are in place.

## Completed
- Established local microservice entrypoints for `gateway-bff`, `identity-service`, `learning-core-service`, `catalog-content-service`, `ai-execution-service`, `tts-media-service`, `asr-service`, `notes-service`, and `admin-ops-service`, plus dedicated ASR Socket.IO runtime on `5001`.
- Provisioned one local PostgreSQL database and one role per service on `127.0.0.1:55432`, and aligned split-service env loading with `backend/.env` plus `backend/.env.microservices.local`.
- Added shared readiness checks and a one-command backend orchestration path via [start-microservices.ps1](/F:/enterprise-workspace/projects/ielts-vocab/start-microservices.ps1), including port reservation and forced process cleanup to avoid Windows port-collision regressions.
- Started Wave 5 by adding a project-owned local Redis baseline on `127.0.0.1:56379`, shared split-service Redis env defaults, and shared Redis URL/readiness helpers in [redis_runtime.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/redis_runtime.py) plus [start-local-redis-microservices.ps1](/F:/enterprise-workspace/projects/ielts-vocab/scripts/start-local-redis-microservices.ps1).
- Extended Wave 5 with a project-owned local RabbitMQ baseline on `127.0.0.1:5679`, shared split-service RabbitMQ env defaults, and shared RabbitMQ URL/readiness helpers in [rabbitmq_runtime.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/rabbitmq_runtime.py) plus [start-local-rabbitmq-microservices.ps1](/F:/enterprise-workspace/projects/ielts-vocab/scripts/start-local-rabbitmq-microservices.ps1).
- Added the first durable eventing skeleton for split services: per-service outbox and inbox tables in [eventing_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/model_definitions/eventing_models.py), shared outbox helpers in [outbox_runtime.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/outbox_runtime.py), and the first contract registry in [domain-event-contracts.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/architecture/domain-event-contracts.md).
- Moved browser compatibility routing behind `gateway-bff`, added downstream readiness probing, and kept `/api/*` and `/socket.io` behavior compatible with the current frontend.
- Localized a large slice of service application logic into `packages/platform-sdk`, including identity session and email helpers, learning-core library and progress wrappers, catalog-content read and confusable wrappers, notes word-note writes, admin detail helpers, and the ai-execution support stack.
- Removed direct `platform-sdk` dependence on high-level monolith service modules such as `services.ai_*`, `services.auth_session_helpers`, `services.books_catalog_service`, `services.books_confusable_service`, `services.books_progress_service`, `services.books_favorites_service`, `services.books_familiar_service`, `services.memory_topics`, `services.runtime_async`, and `services.local_time`.
- Landed a real remote deployment baseline on `119.29.182.134` with nginx, systemd-managed split services, PostgreSQL provisioning, and production env-file conventions documented in [cloud-microservices-deployment.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/operations/cloud-microservices-deployment.md).
- Froze the current remote production baseline for Wave 1 rollout safety in [remote-production-baseline.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/operations/remote-production-baseline.md), including active systemd units, nginx routing, env-file locations, PostgreSQL backup path, and mandatory smoke checks.
- Froze the remaining `platform-sdk -> services.*` coupling inventory in [platform-sdk-coupling-inventory.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/planning/platform-sdk-coupling-inventory.md) and locked the next extraction targets.
- Moved `quick_memory_schedule`, `study_sessions`, and `learning_events` support logic into `packages/platform-sdk`, kept `backend/services/*` compatibility wrappers, and removed `platform-sdk` imports of `services.quick_memory_schedule`, `services.study_sessions`, and `services.learning_events`.
- Moved `learning_stats_service` payload composition into [learning_stats_payload_support.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/learning_stats_payload_support.py), kept [learning_stats_service.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/services/learning_stats_service.py) as a compatibility shell, and switched the split-service stats path to the shared support implementation.
- Added [learner_profile_application.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/services/learner_profile_application.py) as the explicit learner-profile application entrypoint and repointed split-service callers away from the old `services.learner_profile` import path.
- Moved `notes_summary_runtime` into [notes_summary_runtime_support.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/notes_summary_runtime_support.py) and moved notes summary parsing, prompt-building, persistence, and job-serialization helpers into [notes_summary_service_support.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/notes_summary_service_support.py), while keeping backend wrappers for compatibility.
- Added [llm_provider_adapter.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/llm_provider_adapter.py) and repointed `platform-sdk` AI and notes flows away from direct `services.llm` imports so provider access now goes through one explicit adapter boundary.
- Moved `learning_stats_modes` and `learning_stats_breakdowns` into shared support modules, kept backend compatibility wrappers, and repointed both split-service stats code and legacy `learning_stats_service_parts` helpers away from the old monolith helper modules.
- Added explicit adapter boundaries for learner-profile building, study-session repository access, books user-state and personalization repositories, AI repositories, learning repositories, identity repository access, notes repositories, admin repositories, and catalog runtime repositories so `platform-sdk` no longer directly imports non-adapter `services.*` modules.
- Verified that a direct `platform-sdk -> services.*` scan now returns only adapter files, meaning platform code has stopped binding to raw backend module paths outside the adapter layer.
- Re-ran local guardrails after the Wave 2 extraction sweep: `python -m compileall packages/platform-sdk/platform_sdk backend/services`, `pnpm check:file-lines`, and `pytest backend/tests -q` are green, with the backend suite currently at `426 passed`.
- Started Wave 3 by splitting the mixed [books_personalization_repository.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/services/books_personalization_repository.py) ownership into [learning_core_personalization_repository.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/services/learning_core_personalization_repository.py) for favorites/familiar data and [notes_word_note_repository.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/services/notes_word_note_repository.py) for word-note persistence, while keeping the old module as a compatibility shell.
- Added explicit service-owned repository namespace modules in `platform-sdk` for `identity`, `learning-core`, `catalog-content`, `notes`, `ai-execution`, and `admin-ops`, plus a checked-in [service_repository_plan.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/service_repository_plan.py) and regression coverage in [test_service_repository_plan.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/tests/test_service_repository_plan.py).
- Added explicit service-owned model namespaces under [backend/service_models/learning_core_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/learning_core_models.py), [backend/service_models/notes_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/notes_models.py), [backend/service_models/identity_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/identity_models.py), [backend/service_models/ai_execution_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/ai_execution_models.py), [backend/service_models/catalog_content_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/catalog_content_models.py), [backend/service_models/admin_ops_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/admin_ops_models.py), [backend/service_models/learner_profile_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/learner_profile_models.py), and [backend/service_models/ai_route_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/ai_route_models.py), then repointed repositories and service support modules away from direct global `models` imports.
- Removed the remaining direct `backend/services -> models` imports, including the AI route/custom-book/wrong-word stack, quick-memory sync helpers, and confusable lookup helpers, so the service layer now consumes `service_models` namespaces instead of the global ORM barrel.
- Added [service_model_registry.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/service_model_registry.py) and repointed split runtimes plus [service_schema.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/service_schema.py) to resolve `db` through explicit service model namespaces, so the deployed split-service startup path no longer imports `models.db` directly inside runtime/bootstrap modules.
- Replaced monolith [app.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/app.py) startup from raw `db.create_all()` to explicit [bootstrap_monolith_schema](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/service_schema.py) table selection, and repointed monolith auth bootstrap to [service_models/identity_models.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/service_models/identity_models.py), so even the compatibility monolith now follows the shared service table plan instead of bootstrapping all ORM metadata blindly.
- Repointed the AI execution application layer away from direct cross-service repository imports for notes, learning stats, quick-memory, learning events, and learner-profile reads by routing those paths through `platform-sdk` adapters, and locked that boundary with [test_ai_execution_repository_boundaries.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/tests/test_ai_execution_repository_boundaries.py).
- Added [service_migration_plan.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/service_migration_plan.py) plus [describe-service-migration-plan.py](/F:/enterprise-workspace/projects/ielts-vocab/scripts/describe-service-migration-plan.py), and connected the SQLite-to-PostgreSQL migration/parity scripts to the same write-owning service baseline registry.
- Verified the Wave 3 migration baseline path locally against real service PostgreSQL targets in [backend/.env.microservices.local](/F:/enterprise-workspace/projects/ielts-vocab/backend/.env.microservices.local) by running `scripts/migrate-sqlite-to-microservice-postgres.py --bootstrap-only --scope owned`, which bootstrapped `identity-service`, `learning-core-service`, `catalog-content-service`, `notes-service`, and `ai-execution-service` successfully on `127.0.0.1:55432`.
- Updated [cloud-microservices-deployment.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/operations/cloud-microservices-deployment.md) and [remote-production-baseline.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/operations/remote-production-baseline.md) with the explicit Wave 3 migration-baseline commands for remote rollout and parity verification.
- Added explicit Wave 3 internal contracts for cross-service reads and writes: [notes_internal_application.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/notes_internal_application.py) plus [notes_internal_client.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/notes_internal_client.py) now expose `/internal/notes/learning-notes`, while [learning_core_events_application.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/learning_core_events_application.py) plus [learning_core_internal_client.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/learning_core_internal_client.py) now expose `/internal/learning/events`.
- Repointed AI execution assistant and related-note flows away from local notes-table access: [ai_assistant_application.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/ai_assistant_application.py) now persists learning notes and assistant-question events through internal service calls, [ai_related_notes_support.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/ai_related_notes_support.py) now reads note history through the notes internal client, and [ai_metric_support.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/ai_metric_support.py) now records assistant-tool metrics through the learning-core internal event contract.
- Ran a read-only remote smoke on `https://axiomaticworld.com/` after the Wave 3 local refactor slice: homepage, `GET /api/books`, `GET /api/tts/voices`, `GET /api/tts/word-audio/metadata?w=hello`, and Socket.IO polling handshake on `/socket.io/?EIO=4&transport=polling` all returned `200`.
- Closed Wave 2 by adding [test_platform_sdk_service_import_boundaries.py](/F:/enterprise-workspace/projects/ielts-vocab/backend/tests/test_platform_sdk_service_import_boundaries.py) to lock `platform-sdk -> services.*` imports inside boundary modules only, then re-running `python -m compileall packages/platform-sdk/platform_sdk backend/services`, `pnpm check:file-lines`, `pnpm lint`, `pytest backend/tests -q` (`432 passed`), and the same five-endpoint read-only remote smoke flow on `https://axiomaticworld.com/` with all responses returning `200`.
- Closed Wave 6A by adding per-downstream gateway timeout, retry, and circuit-breaker enforcement in [gateway_upstream.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/gateway_upstream.py), applying the same policy to direct TTS and ASR gateway proxy flows through [gateway_media_proxy.py](/F:/enterprise-workspace/projects/ielts-vocab/packages/platform-sdk/platform_sdk/gateway_media_proxy.py), documenting the HTTP `:8106` plus Socket.IO `:5001` ASR deployment split in [asr-http-socketio-contract.md](/F:/enterprise-workspace/projects/ielts-vocab/docs/operations/asr-http-socketio-contract.md), and re-running `pytest backend/tests/test_http_proxy.py backend/tests/test_gateway_bff_ai_proxy.py backend/tests/test_gateway_bff_tts_proxy.py backend/tests/test_gateway_bff_readiness.py backend/tests/test_asr_service_api.py backend/tests/test_asr_socketio_service.py -q` (`28 passed`), `python -m compileall apps/gateway-bff packages/platform-sdk/platform_sdk services/asr-service`, `pnpm check:file-lines`, and `pnpm lint`.
- Re-ran the full local backend regression and guardrails after the latest Wave 3 slice plus branch-integration fixes: `python -m compileall packages/platform-sdk/platform_sdk backend/tests apps/gateway-bff`, `pytest backend/tests -q` (`470 passed`), and `pnpm check:file-lines` are green.

## In Progress
- Turning the split runtime into the canonical backend path in both local and remote environments by keeping service startup, readiness, nginx proxy behavior, and systemd-managed rollout safety green after each refactor slice.
- Continuing Wave 3 from the repository/model cut into migration ownership: the baseline registry and local bootstrap verification are landed, but the next closure is to reduce transitional shadow-table/bootstrap assumptions and convert more cross-domain reads into explicit downstream contracts.
- Converging the backend onto a real microservice delivery target rather than a service-shaped monolith with shared persistence internals, without breaking the already deployed remote single-server production baseline.
- Continuing Wave 5 infrastructure work on this branch with local Redis and RabbitMQ baselines plus durable event tables, while Wave 4 storage-parity closure continues separately on another branch.

## Next
- Wire the first Redis-backed workloads and connect real publisher plus consumer loops to the new RabbitMQ/outbox skeleton without regressing the deployed split backend, then start the admin read-side rebuild and Wave 6B runtime cutover.

## 30-Step Plan
### Wave 1: Remove Remaining Shared Helper Coupling
1. Freeze the current remote production baseline: record active systemd units, nginx routing, env-file locations, PostgreSQL backup path, and the exact smoke checks that must stay green after every rollout.
2. Freeze the remaining `platform-sdk -> services.*` dependency inventory and lock the target ownership list for each helper module.
3. Move `quick_memory_schedule` logic into `platform-sdk` support modules and repoint AI and learning-core callers.
4. Move `study_sessions` helper logic into service-local or shared support modules and remove monolith helper imports from split services.
5. Move `learning_events` recording behind a service-local event adapter so split services stop depending on monolith event helper entrypoints.

### Wave 2: Finish Notes and AI Shared Support Extraction
6. Move `learner_profile` and `learning_stats_service` composition helpers behind explicit service-local application modules.
7. Move `notes_summary_runtime` into `platform-sdk` and keep notes job progress and streaming behavior unchanged.
8. Move `notes_summary_service_parts` parsing, prompt-building, persistence, and job-serialization helpers into `platform-sdk`.
9. Isolate `llm` access behind explicit provider adapter boundaries so split services no longer bind directly to monolith-facing helper modules.
10. Re-run the AI, notes, auth, learning, admin, and gateway regression packs, then execute the same smoke flow against the deployed remote chain before closing the wave.

### Wave 3: Split Shared Repository and Model Ownership
11. Create service-owned repository modules for `identity`, `learning-core`, `catalog-content`, `ai-execution`, `notes`, `tts-media`, `asr`, and `admin-ops`.
12. Split shared SQLAlchemy model declarations into service-owned model packages with clear table ownership.
13. Remove cross-service repository imports from application layers and replace them with owned repositories or explicit downstream calls.
14. Make each service bootstrap only its owned models and tables instead of touching the global ORM set.
15. Generate an initial migration baseline per service and verify bootstrap plus migration commands against both local PostgreSQL and the remote deployment runbook.

### Wave 4: Close Data-Boundary and Storage Gaps
16. Audit every current table against authoritative service ownership and mark all non-owning access as read-only or transitional.
17. Disable new writes to shared SQLite fallback paths for services that already have PostgreSQL ownership.
18. Finish converging media, exports, and temporary binary artifacts onto Aliyun OSS object references with no disk-primary fallback.
19. Add per-service backfill and repair scripts for PostgreSQL rows and OSS object metadata.
20. Run parity validation between legacy SQLite and the new service-owned PostgreSQL and OSS state, then rehearse the same migration and rollback flow for the deployed remote environment.

### Wave 5: Add Classical Microservice Infrastructure
21. Bring up local Redis runtime and shared service configuration for cache, rate-limit, and ephemeral session workloads.
22. Bring up the corresponding remote Redis runtime and bind the deployed services to it with no behavior regressions under nginx and systemd.
23. Bring up local and remote RabbitMQ runtime plus shared service configuration for asynchronous domain events and work queues.
24. Add transactional outbox support to write-owning services so event publication is durable and replayable.
25. Publish the first event contract set for `identity`, `learning`, `notes`, `tts`, and `ai-execution`, then rebuild admin and other cross-domain read models from events instead of shared table reads.

### Wave 6A: Edge Hardening
26. Add gateway timeout, retry, and circuit-breaker policy per downstream service.
27. Finalize ASR HTTP and Socket.IO deployment contracts, gateway streaming pass-through coverage, and reverse-proxy expectations for both local and remote runtime.

### Wave 6B: Canonical Runtime Cutover
28. Remove direct browser access assumptions from legacy monolith startup, make the split-service startup chain the default local backend path, and align the remote systemd rollout path with the same contract.

### Wave 6C: Monolith Retirement and Final Cutover
29. Remove or archive monolith-only route registration, startup paths, and shared-SQLite runtime assumptions only after remote fallback and restart procedures are documented and tested.
30. Run the final cutover validation pack, confirm full frontend compatibility through `gateway-bff` on the deployed remote domain, and declare the split backend the canonical architecture.

## Risks
- The backend is already split at the process boundary, but shared repositories and shared ORM ownership still prevent it from being a fully classical microservice backend.
- Redis and RabbitMQ local baselines plus the outbox/inbox table skeleton are landed, but first live publishers, consumers, and event-driven admin read models are still pending, so cross-service consistency still leans on shared database reads in some paths.
- `gateway-bff`, `nginx`, ASR Socket.IO, and the multi-port backend chain now exist in both local and remote environments, so startup ordering, proxy drift, and rollout mistakes can break a live deployed system instead of only a local dev stack.
- The remote deployment is a single-server production baseline rather than the final Kubernetes form, so every remaining migration step needs backward-compatible rollout and rollback discipline before the architecture evolves again.
- `platform-sdk` is now adapterized, but the adapters still point at shared repositories and shared ORM ownership, so the backend is not yet a classical microservice system until repository ownership and model ownership are split in Wave 3.

